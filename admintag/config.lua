modAdminTag = {
	nametagcolor_normal = { a=255, r=255, g=255, b=255 }, --Normal player (without priv 'server') have white nametag
	nametagcolor_server = { a=255, r=255, g=0	 , b=0   }, --Admin player (with priv 'server') have red nametag.
}
